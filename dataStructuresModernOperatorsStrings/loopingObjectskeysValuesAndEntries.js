// looping Objects: Object keys, Vlaues and Entries

// plain Objects with Object keys, values and entries
const person = {
  name: "tony",
  height: 5.2,
  color: "brown",
};
console.log(Object.keys(person));
console.log(Object.values(person));
console.log(Object.entries(person));

// looping objects by using object keys, values and entries
