'use strict'; // used to wirte more secure code and strict mode is to avoid accidental error ie; avoids bugs in our code 
//also helps to creates visible code error in terminal

let hasDriversLicense = false;
const passTest = true;

if (passTest) hasDriversLicense = true; // to know how strict mode helps to see code error

if (hasDriversLicense) console.log('I can Drive :D');

const interface = 'Audio'; // interface cannot use as a variables
const private = 345; // private word cannot use as variable
//const if = 'dow'; //if cannot use a variable
